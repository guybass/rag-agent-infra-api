# Application
APP_NAME=RAG Agent Infrastructure API
APP_ENV=development
DEBUG=true

# Server
HOST=0.0.0.0
PORT=8000

# Authentication
SECRET_KEY=your-secret-key-change-in-production
API_KEY=your-api-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30
AUTH_DISABLED=false  # Set to true to disable auth (for testing in private VPC)

# LLM Provider Configuration
# Options: bedrock, openai, anthropic
LLM_PROVIDER=bedrock

# AWS Bedrock (used when LLM_PROVIDER=bedrock)
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=us.anthropic.claude-sonnet-4-5-20250929-v1:0

# AWS Authentication (choose ONE method):
#
# Option 1: EC2 Instance Role (RECOMMENDED for EC2)
#   - Leave AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY empty/commented
#   - The EC2's attached IAM role will be used automatically
#
# Option 2: Assume a different IAM Role
#   - Set AWS_ASSUME_ROLE_ARN to the role you want to assume
#   - Your EC2's role must have sts:AssumeRole permission for this role
# AWS_ASSUME_ROLE_ARN=arn:aws:iam::123456789012:role/EC2-Admin-Role
#
# Option 3: Explicit credentials (NOT recommended for EC2)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key

# OpenAI (used when LLM_PROVIDER=openai)
OPENAI_API_KEY=your-openai-api-key

# Anthropic (used when LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Redis
REDIS_URL=redis://localhost:6379/0

# ChromaDB
CHROMA_PERSIST_DIRECTORY=./chroma_data
CHROMA_COLLECTION_NAME=documents

# Terraform Storage
TERRAFORM_STORAGE_PATH=./terraform_data

# Document Processing
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
MAX_UPLOAD_SIZE_MB=50
