# AWS Region
aws_region = "us-east-1"

# Project name (used for resource naming)
project_name = "rag-agent"

# EC2 instance type
# t3.medium = 2 vCPU, 4GB RAM (~$30/month)
# t3.large  = 2 vCPU, 8GB RAM (~$60/month) - better for large ChromaDB
instance_type = "t3.medium"

# Your SSH key pair name (must exist in AWS)
key_name = "your-key-name"

# Restrict SSH access to your IP (recommended!)
# Find your IP: curl ifconfig.me
allowed_ssh_cidr = "YOUR_IP/32"  # e.g., "203.0.113.50/32"
